diff --git a/web/app/portal/dashboard/PortalDashboardClient.tsx b/web/app/portal/dashboard/PortalDashboardClient.tsx
index 66c82ed..16f254f 100644
--- a/web/app/portal/dashboard/PortalDashboardClient.tsx
+++ b/web/app/portal/dashboard/PortalDashboardClient.tsx
@@ -122,8 +122,11 @@ export default function PortalDashboardClient({ member, eeg, invoices, documents
   const [appError, setAppError] = useState<string | null>(null);
   const [appRemaining, setAppRemaining] = useState(0);
   const [isMobile, setIsMobile] = useState(false);
+  const [isIOS, setIsIOS] = useState(false);
   useEffect(() => {
-    setIsMobile(/iphone|ipad|ipod|android/i.test(navigator.userAgent));
+    const ua = navigator.userAgent;
+    setIsMobile(/iphone|ipad|ipod|android/i.test(ua));
+    setIsIOS(/iphone|ipad|ipod/i.test(ua));
   }, []);
   const loadAppToken = useCallback(() => {
     setAppLoading(true);
@@ -900,7 +903,7 @@ export default function PortalDashboardClient({ member, eeg, invoices, documents
                   Scannen Sie diesen Code mit der <strong>EEG GrünLicht App</strong> — oder tippen Sie
                   den Code darunter ein.
                 </p>
-                {isMobile && appQr.app_link && (
+                {isMobile && !isIOS && appQr.app_link && (
                   <a
                     href={appQr.app_link}
                     className="w-full text-center px-4 py-3 text-sm font-semibold rounded-lg bg-green-700 text-white hover:bg-green-800 transition-colors"
@@ -908,6 +911,16 @@ export default function PortalDashboardClient({ member, eeg, invoices, documents
                     In der EEG GrünLicht App öffnen
                   </a>
                 )}
+                {isIOS && (
+                  <div className="w-full rounded-lg border border-slate-300 bg-slate-50 px-4 py-3 text-sm text-slate-700">
+                    <strong>Am iPhone:</strong> Öffne die <strong>EEG GrünLicht App</strong> und tippe
+                    „Mit E-Mail verbinden“ — oder gib den 8-stelligen Code unten unter
+                    „Code manuell eingeben“ ein.{" "}
+                    <span className="text-slate-500">
+                      (Der Button „In der App öffnen“ kann die App auf dem iPhone systembedingt nicht öffnen.)
+                    </span>
+                  </div>
+                )}
                 {/* eslint-disable-next-line @next/next/no-img-element */}
                 <img
                   src={appQr.qr}
