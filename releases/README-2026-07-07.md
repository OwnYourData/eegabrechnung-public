# Release 2026-07-07

**Upstream:** `lutzerb/eegabrechnung-public` @ `cd2c59d`
**Vorgänger (Basis dieses Forks):** `v2026-07-05` (`16fd58a`)
**Umfang:** 61 Dateien, +2.379 / −645 Zeilen (Vendor-Code)

Überblick der Änderungen zwischen `v2026-07-05` und diesem Release. Upstream veröffentlicht keine eigene Changelog-Historie; dieser Eintrag wird aus `git diff v2026-07-05 upstream/main` abgeleitet.

## Neue Migrationen (082–086) = neue Features

| Migration | Inhalt |
|-----------|--------|
| 082 | **SEPA-Mandat-Historie:** neue Tabelle `member_sepa_mandate_history` — archiviert bei Mandatsänderung die bisherigen Mandatsdaten (IBAN, `signed_at`/`signed_ip`/`signed_text`, `reason`), indexiert nach `(member_id, archived_at DESC)` |
| 083 | **Fee-Billing-Mode:** `fee_billing_mode` auf `eegs` (`per_month` \| `per_invoice`) — steuert, ob Fixgebühren pro Monat oder pro Rechnung verrechnet werden |
| 084 | **Billing-Run-Scope:** `billing_type` + `member_ids UUID[]` auf `billing_runs` — Abrechnungsläufe können auf ausgewählte Mitglieder eingegrenzt werden |
| 085 | **Mitglieds-E-Mail-Änderung:** neue Tabelle `member_email_change_verifications` (Magic-Token, `expires_at`, partieller Unique-Index auf offene Änderungen pro Mitglied) — Self-Service im Mitgliederportal |
| 086 | **Energie-Imbalance-Schwelle:** `energy_imbalance_threshold_promille` auf `eegs` (Default 1 ‰) — Schwellwert für die Erkennung/Meldung von Bezug-/Einspeise-Ungleichgewicht |

## Weitere wesentliche Neuerungen

- **Mitgliederportal – Self-Service „Profil"** (`member_portal.go` +360, `PortalDashboardClient.tsx` +205): neuer **Profil-Tab** mit E-Mail-Adressänderung (Anforderung + Magic-Token-Bestätigung über `portal/email-change` und `portal/email-change/confirm`) sowie SEPA-Mandat-Download (`portal/sepa-mandate`). Neue Web-Routen darunter, neue Seite `web/app/portal/email-change/confirm/page.tsx`.
- **Scoped Billing-Runs:** `repository/billing_run.go` (+65) und `billing-run-form.tsx` (+23) — Abrechnung wahlweise für alle oder nur ausgewählte Mitglieder (`billing_type`/`member_ids`).
- **E-Mail-Header-Encoding:** neues Paket `api/internal/mailutil/header.go` (+ Test) — korrektes MIME-Encoding von Betreff/Absender (Umlaute); eingebunden in `invoice/email.go`.
- **SEPA/CAMT-Ausbau:** `handler/sepa.go` (+80) plus neuer Test `sepa_camt053_test.go` — robustere Zahlungszuordnung.
- **Tarif-/Mitglieds-/Onboarding-Anpassungen:** `handler/tariff.go` (+111), `handler/member.go` (+105), `handler/onboarding.go` (+116), `settings/page.tsx` (+61), `member/[memberId]/page.tsx` — u. a. rund um Fee-Billing-Mode, Imbalance-Schwelle und SEPA-Mandat-Historie.
- **Rechnungs-PDF/Import-Feinschliff:** `invoice/pdf.go`, `importer/energiedaten.go`, diverse Repository-Ergänzungen (`member.go`, `member_portal.go`, `reading.go`, `eeg.go`).

## Kompatibilität der eigenen Patches

Geprüft mit `git apply --check` gegen diesen Release-Stand (alle drei Patches zusammen: **OK ✅**).

| Patch | Status | Anmerkung |
|-------|--------|-----------|
| `0002-billing-preview-columns` | ✅ **wendet sauber an** | Upstream hat `billing-run-form.tsx` zwar erweitert (+23, Scoped Billing), der Patch-Kontext passt aber weiterhin |
| `0003-meterpoint-update-registriert-seit` | ✅ **wendet sauber an** | `meter_point.go`/`meterpoint.go` upstream verändert, Patch-Kontext bleibt gültig |
| `0004-portal-app-connect-qr` | 🔧 **überarbeitet (gegen 07-07 neu erstellt)** | Upstream hat `PortalDashboardClient.tsx` stark umgebaut (neuer **`profil`-Tab**) und den Portal-Routenblock in `main.go` erweitert. Die beiden In-Place-Edits wurden neu generiert (activeTab-Union um `profil` + `app` ergänzt, „App verbinden"-Button jetzt nach dem Profil-Tab). Die neuen Dateien (`portal_app_token.go`, `route.ts`, `go.mod`/`go.sum`) sind unverändert. `portalAuth`-Helper und `change-factor`-Route existieren weiterhin. |

**To do vor dem Deploy:** Patches `0002`, `0003`, `0004` anwenden (`git apply patches/*.patch`). Patch `0004` liegt bereits in der gegen 07-07 überarbeiteten Fassung im Repo.
