# Release 2026-07-05

**Upstream:** `lutzerb/eegabrechnung-public` @ `16fd58a`
**Vorgänger (Basis dieses Forks):** `v2026-06-16` (`e8bb11c`)
**Umfang:** 118 Dateien, +6.583 / −1.101 Zeilen

Überblick der Änderungen zwischen `v2026-06-16` und diesem Release. Upstream veröffentlicht keine eigene Changelog-Historie; dieser Eintrag wird aus `git diff v2026-06-16 upstream/main` abgeleitet.

## Neue Migrationen (070–081) = neue Features

| Migration | Inhalt |
|-----------|--------|
| 070 | **Zählpunkt-Recycling (Mieterwechsel):** globaler `UNIQUE(zaehlpunkt)` entfällt, partieller Index `(eeg_id, zaehlpunkt) WHERE abgemeldet_am IS NULL` — derselbe Zählpunkt kann nach Abmeldung neu vergeben werden |
| 071 | **EDA-Response-Info + §16e-Smartmeter-Benachrichtigung:** `response_codes`, `meter_owner_name`, `portal_approval_url`, `customer_notified_at` auf `eda_processes`; Code 182 (Zähler nicht fernauslesbar) löst automatische Mitglieder-Mail aus |
| 072 | **Bank-Import-Duplikatschutz** (mit Referenz): Unique-Index auf `ea_banktransaktionen` |
| 073 | Bank-Feld `auftraggeber_empfaenger` auf unbegrenztes TEXT erweitert |
| 074 | **Bank-Import-Duplikatschutz** (ohne Referenz, über Verwendungszweck) |
| 075 | `paid_at` auf Rechnungen — gesetzt durch CAMT.053-Zahlungsabgleich |
| 076 | **Individualtarife:** mitgliedsspezifische Tarif-Overrides; `zaehlpunkts_gebuehr_eur` (Gebühr × aktive Zählpunkte) |
| 077 | **Registrierungshistorie:** `meter_point_registration_periods` — vollständige An-/Abmelde-Historie pro Zählpunkt |
| 078 | `zaehlpunkt` auf `eda_messages` |
| 079 | Erkennung von Energierichtungs-Konflikten am Zählpunkt (direction mismatch) |
| 080 | **Zahlungshinweis-Modus** auf Rechnungen: `sepa_lastschrift` \| `ueberweisung` \| `none` |
| 081 | **Verteilungsmodell (ECDisModel):** statisch/dynamisch pro Gemeinschaft |

## Weitere wesentliche Neuerungen

- **ML-Prognose-Service** (`forecast/` – neu): 7-Tage-Prognose als eigener Docker-Service (`docker compose --profile forecast`); `handler/forecast.go`, Web-Route + Toggle auf der Reports-Seite.
- **CAMT.053-Zahlungsabgleich:** `handler/sepa.go`, Route `billing/camt053`, Upload-Komponente `billing-camt053-upload.tsx` — matcht Zahlungen über EndToEndId gegen Rechnungen (`status=paid`, `paid_at`).
- **Bulk-Mail-Performance:** `invoice/smtp_bulk.go` — eine SMTP-Verbindung pro Sendelauf statt pro Mail (behebt Timeout bei ~50+ Rechnungen).
- **Onboarding-Erweiterungen:** E-Mail-Bestätigung (`onboarding/confirm/[token]`, `ConfirmForm`), manueller Onboarding-Link (`onboarding/manual`, `OnboardingManualCreateModal`).
- **EDA-Ausbau:** große `eda/worker.go`-Erweiterung (+430) — Response-Codes, Smartmeter-Benachrichtigung, unaufgeforderte Widerrufe; neue Web-Bibliotheken `eda-error-codes.ts` (Code-Klartext, z. B. 55/182) und `eda-status-labels.ts`; überarbeitete `eda-processes-table.tsx`/`eda-messages-table.tsx`.
- **Individualtarife-UI:** Tarifseiten/-Routen pro Mitglied, `tariff-manager.tsx`.
- **Registrierungshistorie-UI:** Zählpunkt-`history`-Route, `registration-timeline`, `ZaehlpunktAbmeldenSection`.
- **Neue UI-Komponenten:** `meter-point-status-badge.tsx`, `member-combobox.tsx`; erweiterte `energy-charts.tsx`, `import-sections.tsx`, `file-upload.tsx`.
- **Stammdaten-Import** (`importer/stammdaten.go`, +153) und **Rechnungs-PDF** (`invoice/pdf.go`, +142) überarbeitet.

## Kompatibilität der eigenen Patches

Geprüft mit `git apply --check` gegen diesen Release-Stand.

| Patch | Status | Anmerkung |
|-------|--------|-----------|
| `0002-billing-preview-columns` | ✅ **wendet sauber an** | Zieldatei `web/components/billing-run-form.tsx` ist upstream unverändert |
| `0001-einspeisung-ohne-anpassungsfaktor` | 🗑️ **obsolet — von Upstream übernommen** | Upstream 07-05 hat die proportionale Einspeise-Skalierung **komplett entfernt**: `SumByMemberAndPeriod`/`SumForMember` liefern jetzt `wh_community` roh (ohne `* total_consumer_self / total_generator_community`, siehe Code-Kommentar „intentionally left visible rather than smoothed over"). Das „ohne Anpassungsfaktor"-Verhalten des Patches ist damit der neue **Standard**. Der Patch wird nicht mehr gebraucht. |

**Wichtige Semantik-Änderung:** Ab 07-05 wird die Einspeisung generell **roh/unskaliert** vergütet (kein Anpassungsfaktor mehr — auch nicht optional). Das entspricht genau dem, was Patch 0001 bezweckte, gilt jetzt aber unbedingt für jeden Abrechnungslauf; einen „skalierten" Modus gibt es nicht mehr. Auf die Rechnungsbeträge wirkt sich das gegenüber dem alten 06-16-Standard aus (Einspeise-kWh = gemessene Werte statt hochskaliert).

**To do vor dem Deploy:** nur Patch `0002` anwenden. Patch `0001` stilllegen (nach `patches/retired/` verschieben — siehe unten).
